apiVersion: apps/v1beta1
kind: Deployment
metadata:
  name: user
spec:
  replicas: 1
  strategy:
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  minReadySeconds: 5
  template:
    metadata:
      labels:
        app: user
    spec:
      containers:
        - name: user
          image: eu.gcr.io/s62-1-278920/user
          ports:
            - containerPort: 49502
          resources:
            requests:
              cpu: 100m
            limits:
              cpu: 100m